using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace Data.DoorHomeworkAPI.Models
{
    //public class DictionaryStorage : IModelStorage
    //{
    //    private Dictionary<long, Door> items = new Dictionary<long, Door>();

    //    public Door this[long key] { get { return items[key]; } set { items[key] = value; } }

    //    public IEnumerable<Door> Items => items.Values;

    //    public bool ContainsKey(long key) => items.ContainsKey(key);

    //    public void RemoveItem(long key) => items.Remove(key);
    //}
}
